// Price List Screen Placeholder
import 'package:flutter/material.dart';

class PriceListScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Price List Screen')),
    );
  }
} 