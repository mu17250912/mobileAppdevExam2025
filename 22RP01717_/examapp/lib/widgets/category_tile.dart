import 'package:flutter/material.dart';

class CategoryTile extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return ListTile(
      title: Text('Category Tile Placeholder'),
    );
  }
} 